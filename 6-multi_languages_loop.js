// Array of strings
const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Loop through the array using a classic for loop
for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}